{
  "name": "mescript-labs-monorepo",
  "version": "1.0.0",
  "description": "Mescript Labs monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  }
}
